@{
    ViewBag.Title = "Welcome";
    Layout = "~/Views/Shared/_AuthLayout.cshtml";
}

<h1>Welcome</h1>
<p class="text-muted mb-4">Atlas Premier Properties. Choose how you'd like to sign in.</p>

<div class="d-grid gap-3">
    <a href="@Url.Action("OwnerLogin", "Account")" class="btn btn-primary btn-lg">Property owner</a>
    <a href="@Url.Action("Login", "Account")" class="btn btn-secondary btn-lg">Staff</a>
</div>
